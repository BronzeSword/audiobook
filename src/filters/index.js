// eslint-disable-next-line func-names
const articleStatus = function (value) { // 文章审核状态
    // eslint-disable-next-line radix
    switch (parseInt(value)) {
    case 1:
        return '';
        default: return '';
    }
};